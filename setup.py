from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A follow up work to MAPFAST with graph neural networks and a larger portfolio',
    author='Eric Ewing',
    license='MIT',
)
